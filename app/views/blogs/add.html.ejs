<div class="hero-unit">
  <p>Nb: status == 'published' shows up only.  All other strings => ignored post.</p>
  <p>Author is just a text field, no ties to users. Please use lowercase 'alex' 'tim' 'ryan'!</p>
  <p>Blog text is in <a href="http://daringfireball.net/projects/markdown/syntax">markdown</a></p>
</div>
<div class="hero-unit">
  <form id="blog-form" class="form-horizontal" action="/blogs" method="POST">
    <fieldset>
      <legend>Create a new Blog</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {blog: {postDate: new Date()}}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
